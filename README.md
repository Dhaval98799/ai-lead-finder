# ai-lead-finder
Sign up → n8n.io (free cloud trial) Get SerpAPI key → serpapi.com Get Claude API key → console.anthropic.com Get Snov.io API key → app.snov.io → Settings → API Import the workflow template from n8n's marketplace: search "Lead Enrichment"
